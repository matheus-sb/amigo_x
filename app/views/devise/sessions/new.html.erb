<div class="container">
    <div class="row lead" style="text-align: center">
        <h2 class="auth-h2">Amigo X</h2>
    </div>
    <div class="auth-sub-title row lead">
        O jeito mais fácil de organizar um amigo oculto
    </div>
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Fazer login para acessar o Amigo X</h3>
                </div>
                <div class="panel-body">
                  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {role:"form"}) do |f| %>
                    <fieldset>
                      <div class="form-group">
                        <!-- <%= f.label :email %><br /> -->
                        <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "E-mail" %>
                      </div>

                      <div class="form-group">
                        <!-- <%= f.label :password %><br /> -->
                        <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "Senha" %>
                      </div>

                      <% if devise_mapping.rememberable? -%>
                        <div class="checkbox">
                          <label>
                            <%= f.check_box :remember_me %>Lembrar-me
                          </label>                              
                        </div>
                      <% end -%>

                      <!-- <div class="actions"> -->
                        <%= f.submit "Entrar", class: "btn btn-lg btn-primary btn-block" %>
                      <!-- </div> -->
                    </fieldset>
                  <% end %>

                  <%= render "devise/shared/links" %>
              </div>
            </div>
        </div>
    </div>
</div>
